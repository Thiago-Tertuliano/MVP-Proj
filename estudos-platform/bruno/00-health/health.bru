meta {
  name: Health
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/health
  body: none
  auth: none
}

vars:pre-request {
  baseUrl: http://localhost:8080
}

assert {
  res.status: eq 200
  res.body: eq ok
}

docs {
  Smoke test. A API precisa estar no ar (`go run ./cmd/api`).
}
